# 한국투자증권 API 호출 제한 설정
# 필요에 따라 값을 수정하고 .env 파일에 추가하세요

# 개별 API 타입별 제한 (동일 TR 기준)
ORDER_API_CALLS_PER_SEC=2
MARKET_DATA_API_CALLS_PER_SEC=2
ACCOUNT_API_CALLS_PER_SEC=2

# 전체 API 통합 제한 (전체 동시 TR 기준)
TOTAL_API_CALLS_PER_SEC=8

# 일일 호출 제한 (환경별)
MOCK_DAILY_API_LIMIT=10000
LIVE_DAILY_API_LIMIT=20000

# 사용법:
# 1. 위 값들을 .env 파일에 복사
# 2. 필요에 따라 숫자만 변경
# 3. 애플리케이션 재시작
# 
# 예시: 더 보수적으로 설정하고 싶다면
# ORDER_API_CALLS_PER_SEC=1
# MARKET_DATA_API_CALLS_PER_SEC=1
# TOTAL_API_CALLS_PER_SEC=5 